const SaveSection = () => {

    return (
        <section id="complete">
            Finalizing palette section here. Download and save buttons (navigating to Create component)
        </section>
    );
};

export default SaveSection;